skip_list:
  - '106' # Role name {} does not match ``^[a-z][a-z0-9_]+$`` pattern'

exclude_paths:
  - meta/exception.yml
  - meta/preferences.yml
  - molecule/default/verify.yml
  - molecule/http_only/verify.yml
